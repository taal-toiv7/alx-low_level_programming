#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Starting Block (void)
 *
 * Return: Value 0 (pass)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");

	return (0);
}
