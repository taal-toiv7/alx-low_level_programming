#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Starting Block (0)
 *
 * Return: Value 0 (Pass)
 */

int main(void)
{
	char low;

	for (low = 'a'; low <= 'z'; low++)
		putchar(low);
		putchar('\n');

	return (0);
}
